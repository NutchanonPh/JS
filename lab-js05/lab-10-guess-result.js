function sayHi(age) {
    if (age < 12) alert('Hi kid');
}
console.log(sayHi); // *
console.log(sayHi(10)); // **
// บรรทัด * แสดงผลลัพธ์เป็น if (age < 12) alert('Hi kid');  บรรทัด ** แสดงผลลัพธ์ Hi kid 